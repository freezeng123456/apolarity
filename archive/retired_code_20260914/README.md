# Retired source snapshots

These exact source snapshots are retained in `retired_sources.zip`, with per-file SHA-256 checksums. They include the obsolete automatic Waring/polarization selector and superseded fixed-constraint training entry points. They are excluded from the installed package and the current paper workflow. Do not use them to reproduce the current numerical experiments.

The current PDE entry is `experiments/torch_benchmarks/train_fixed_wall.py`. Historical paper results and their frozen source under `results/paper_resampled_20260914/` are unchanged.
