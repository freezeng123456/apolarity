# CH-only paired rerun, learning rate 1e-4

User requests both CH derivative implementations rerun at constant Adam learning rate1e-4. Preserve T4-B, four128-wide tanh hidden layers, batch400, paired seed20260918,1200 seconds per method, exact source and constraints, loss weights, fixed random evaluation points and offline diagnostics. Two formal cells only; start from original initialization, not checkpoints. No KdV reruns. Total40minutes timed training plus two short smoke cells and diagnostics. Invoke run_fixed_wall_matrix.py --cases ch2d --hidden-layers 4 --lr 1e-4 --seconds 1200 --out <fresh-root>. Abort on failed gate; no extra seeds or automatic retries.
