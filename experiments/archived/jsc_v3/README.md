# Archived JSC v3 protocol sources

这里保存已经退出当前论文范围的 JSC v3 task registry、边界权重、Poly family
入口和协议测试。对应 runner、日志、结果与论文分别位于 archive 的
`scripts/`、`logs/jsc_v3/`、`results/double_protocols/jsc_v3/` 和
`docs/archive/paper_jsc_legacy/`。

这些文件只用于追溯旧协议，不是当前 Poly/CH2D 正式入口，也不保证在新目录下
单独可执行。
