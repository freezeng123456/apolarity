# IC/BC resampling rerun, 2026-09-14

The user requests that every paper training comparison with fixed initial or boundary training points be rerun on SCNet with those points resampled each update. This campaign replaces the sampling protocol, not the PDE, derivative backends, objective weights, architecture, optimizer, or evaluation sets. Earlier completed results remain intact and are historical controls.

Main matrix: kdv1d, kdv2d, ch2d x nested_jvp/shared_jet_linear x seeds 20260919--20260923 = 30 cells. Each uses 1200 s requested training wall time, four width-128 tanh hidden layers (depth=5), batch=400, Adam 1e-5 for KdV and 1e-4 for CH, evaluation seed base 20261400, 10000 spacetime and 10000 terminal evaluation points, full checkpoints every 10 s and model states every 2 s. Include IC/BC sampling in the timed training loop. Each cell has one exclusive GPU; maximum requested concurrency 28, further reduced by live resource and account limits.

Appendix matrix: 1D KdV seed 20260918 x two methods; 2D KdV seeds 20260921--20260923 x two methods = 8 cells. Preserve three width-128 hidden layers, 10000 updates, Adam linear decay from 1e-3 to zero, batch=400, assessment every 50 updates, and the original evaluation grids. These reruns use SCNet RTX 3080 / PyTorch 2.5.1; do not pool their timings with the earlier T4 runs.

Training constraints: independently uniform points on each original constrained surface, newly drawn on every update. Keep 128 points per surface for 1D and 256 points per surface for 2D. Existing multiple penalties on the same surface share that surface's sampled points. Preserve the prescribed KdV2D surfaces; do not add a y=+1 penalty. For CH preserve all four faces and first/third derivative penalties.

Use a dedicated per-run IC/BC RNG derived from [seed, 0x49434243], separate from the unchanged interior RNG seed+1 and evaluation streams. Both methods use the same initial weights and exactly matching interior and constraint batches at each common update index. Store and audit both batch-hash sequences and the constraint RNG state in checkpoints. Original fixed grids remain diagnostic-only data when resampling is enabled. Report held-out initial/boundary errors separately from training loss; do not infer overfitting solely from the existence of a fixed grid.

Preflight: full local test suite; one exclusive SCNet GPU verifies pinned environment, full-size paired losses/gradients and short training for all entry points before formal jobs. Slurm limits: 10 min preflight, 25 min per main cell, 60 min per appendix cell. No automatic retry of completed/partially trained cells, seed expansion, learning-rate search or replacement of old results. Fail on nonfinite losses/gradients, mismatched source/runtime or paired streams. Formal completion requires all 38 cell markers, successful Slurm exit states, checksum inventory, complete final metrics, paired-stream audit and recovered artifacts. Fixed derivative-value benchmarks in Section 4.1 are unaffected.
